# Use npm for better dependency resolution
package-lock=true

# Include optional dependencies for Linux builds
include=optional

# Use legacy peer deps for better compatibility
legacy-peer-deps=true

# Force install optional dependencies
force=true
